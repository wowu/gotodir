#!/bin/bash

# Run shell completion only once
if [[ $1 == '--setup']]; then
  goto-main --setup
  exit 0
fi

result=$(goto-main path $@)

if [[ $result == /* ]]; then
  cd $result;
else
  goto-main $@;
fi
